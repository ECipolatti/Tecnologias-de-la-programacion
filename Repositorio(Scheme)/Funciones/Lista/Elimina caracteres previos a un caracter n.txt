(define elimantes
  (lambda (elemento lista)
    (if (null? lista)
        '()
        (if (eqv? elemento (car lista))
            (cons elemento (cdr lista))
            (elimantes elemento (cdr lista))
            )
        )
    )
  )
(elimantes 3 '(1 1 1 3 4 5))