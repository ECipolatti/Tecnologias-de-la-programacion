(define elimpos
  (lambda (posicion lista)
    (if (null? lista)
        '()
        (if(eqv? posicion 1)
           (cdr lista)
           (cons (car lista)(elimpos (- posicion 1)(cdr lista)))
           )
        )
    )
  )
(elimpos 3 '(4 8 91 2 7 5))